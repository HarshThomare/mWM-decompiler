Reply with only JSON. Use only these keys. If unsure use "unknown".

Emit one C function for this verified typed µWM subgraph. No loops. Use uint32_t. Operators only: & | ^ ~ + ?:

Subgraph:
{ir}

Names:
{names}

Return: {"c": "<function or unknown>"}
